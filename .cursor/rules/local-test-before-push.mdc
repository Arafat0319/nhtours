---
description: 改版后先本地自测，经用户确认后再 push 部署
alwaysApply: true
---

# 本地测试后再 push

- **禁止**未经用户明确同意就 `git push origin main`（触发生产 Deploy）
- 改完给出本地验证步骤（`cd flask-app && python run.py`，8080）
- 用户确认后再 push；push 前 context 见 **`context-sync-before-push.mdc`** 与 **`context/11`**

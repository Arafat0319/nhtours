---
description: 改版后先本地自测，经用户确认后再 push 部署
alwaysApply: true
---

# 本地测试后再推送（必须遵守）

本项目的 CI/验证在**本地**完成；GitHub Actions **Deploy** 仅在 `git push origin main` 后触发，**不是**上线前的测试环节。

## 禁止

- 完成改版、修复或新功能后，**不得**未经用户明确同意就执行：
  - `git push` / `git push origin main`
  - 任何会触发生产自动部署的操作
- 不得假设「改完就部署」；默认停留在**本地可测状态**（已 commit 或未 commit 均可，以用户习惯为准）。

## 必须

1. 改完代码后，给出**本地验证步骤**（如何启动、访问哪些 URL、预期结果）。
2. 明确告知用户：「请你在本地测试；确认无误后告诉我，我再帮你 push / 部署。」
3. 仅在用户明确表示测试通过、同意推送（如「可以 push」「部署吧」「测试没问题」）后，才执行 `git push origin main`。
4. 若用户只要求改代码、未要求部署，**只改代码 + 更新 context（若需要）**，不 push。

## 与用户确认时的表述示例

> 改动已完成，请本地运行 `python run.py`（端口 8080）验证 XXX。确认无误后回复我，我再 push 到 main 触发 Deploy。

## 相关文档

- 本地开发：`context/06_部署指南.md`
- 防灾与 Git：`context/10_防灾与备份机制.md`

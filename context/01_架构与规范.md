# 架构与规范

本文档定义项目的目录结构、编码规范和开发协作规则。

## 目录

- [目录结构](#目录结构)
- [编码规范](#编码规范)
- [设计模式](#设计模式)
- [开发协作规则](#开发协作规则)

---

## 目录结构

```
nhtours-website/
├── 手册/                   # 人工文档（用户手册、Agent 开场白）
├── context/                # AI / 开发者技术文档
├── .github/workflows/
├── flask-app/
│   ├── app/
│   │   ├── admin/
│   │   ├── routes.py       # 前台 main 蓝图
│   │   ├── models.py
│   │   └── ...
│   ├── migrations/
│   ├── run.py              # 本地 8080
│   └── wsgi.py
└── .cursorrules
```

---

## 编码规范

### Python

- 遵循 **PEP 8** 风格指南
- 使用 **Type Hinting** 增加代码可读性
- 使用 **工厂模式**: 始终通过 `create_app()` 创建应用实例
- 使用 **蓝图组织**: 前台 `main`（`app/routes.py`）、后台 `admin`（`app/admin/routes.py`，含认证）

```python
# 示例：函数定义
def calculate_booking_total(booking: Booking) -> dict:
    """
    计算 Booking 的总金额
    
    Args:
        booking: Booking 对象
        
    Returns:
        dict: 包含 subtotal, discount, total 等字段
    """
    pass
```

### 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| Python 文件 | 小写 + 下划线 | `admin_routes.py` |
| Python 类 | 大驼峰 | `BookingParticipant` |
| Python 函数/变量 | 小写 + 下划线 | `calculate_fee()` |
| HTML 文件 | 小写 + 连字符 | `trip-detail.html` |
| URL 路由 | 小写 + 连字符 | `/admin/trips/new` |
| JavaScript 变量 | 驼峰 | `bookingData` |
| CSS 类 | Tailwind 风格或 BEM | `btn-primary` |

### HTML/CSS (前端)

- **Tailwind CSS**: 优先使用 Utility Classes，避免手写原生 CSS
- **WeTravel Theme 扩展**:
  - Primary Cyan: `text-wetravel-cyan`, `bg-wetravel-cyan` (#00D1C1)
  - Secondary Green: `wetravel-green` (#4ADE80)
- **Jinja2**:
  - 继承 `base_wetravel.html` 用于后台页面
  - 使用 `{% block %}` 分离内容区域

### JavaScript

- **Vanilla JS**: 保持简单，直接操作 DOM，不使用重型框架
- **No Inline JS**: 将 JS 代码放在 `{% block scripts %}` 或单独文件
- **Data Passing**: 后端数据通过 `data-*` 属性或隐藏 Input 传递

```html
<!-- 示例：后端数据传递 -->
<input type="hidden" id="trip-data" value='{{ trip_json | safe }}'>
```

---

## 设计模式

### Flask 应用工厂模式

```python
# app/__init__.py
def create_app(config_name=None):
    app = Flask(__name__)
    app.config.from_object(config[config_name])
    
    # 注册蓝图
    from app.admin import routes as admin_routes
    app.register_blueprint(admin_routes.bp, url_prefix='/admin')
    
    return app
```

### Trip Builder 分步向导模式

Trip Builder 采用 **Stepper 模式**：

1. **State Management**: 每步提交 Form，数据保存到 Trip 或关联表
2. **Validation**: 后端 WTForms 验证 + 前端 HTML5/JS 验证
3. **JSON Handling**: 复杂数据（支付计划、问卷）在前端组装为 JSON，存入 Hidden Input

进行表单或数据库改动时，请参考 [09_表单与数据库改动框架.md](09_表单与数据库改动框架.md) 的核查清单，确保数据链路畅通。

### 数据库操作

- 所有数据库操作通过 **SQLAlchemy ORM** 进行，禁止手写 SQL
- 合理配置 `backref` 和 `lazy='dynamic'` 优化查询性能

---

## 开发协作规则

### 前端视觉改动须用户同意

在测试、优化、排查、执行等**任何**行为中：

- **禁止**擅自改动页面前端视觉效果（布局、文案呈现、颜色、间距、组件样式、影响观感的模板/CSS/前端 JS）
- 若功能或安全修复可能影响视觉：先说明拟改内容，**征得用户同意后再改**
- 不改变观感的后端 / API / 权限 / 测试脚本改动不受此限
- Cursor 规则：`.cursor/rules/no-frontend-visual-without-approval.mdc`
- 后台角色：`app/admin/decorators.py`（`admin_required`）；测试：`flask-app/tests/` + `pytest.ini`

### 改动影响溯源（核心准则）

设计新功能或改既有逻辑时：

- **须溯源**对其它模块/流程的影响（模型、webhook、账本、名额、邮件、定时任务、前端轮询等）
- 有影响则判断：兼容增强 / 需迁移 / **潜在 bug** / **性能风险**
- 后两类须修或补测试/日志，勿 silent ship；支付类须验证 **付成功 → Manage 有单 → 有邮件**
- Cursor 规则：`.cursor/rules/change-impact-analysis.mdc`

- **改代码前**：需求不清先问；大改动先列计划；用户确认后再动手（详见 `.cursor/rules/`）。
- **push / 部署**：本地测完 → 用户明确同意 → 按 [11_推送与上下文同步.md](11_推送与上下文同步.md) 更新 context → 同一 commit push。细则见 `.cursorrules`。
- **防灾**：Git 覆盖工作区前必读 [10_防灾与备份机制.md](10_防灾与备份机制.md)。
- **验证**：改完给出本地步骤（`python run.py`，8080）。
- **安全**：CSRF、输入验证、敏感信息走环境变量。

### Git 提交格式

```
[模块] 简短描述

- 改动内容与原因
验证: …
```

---

## 更新日期

**最后更新**: 2026-08-17（新增：改动影响溯源核心准则）

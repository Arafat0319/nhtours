# 项目概览

NH Tours 是一个旅游管理系统，从原有的静态 HTML 网站重构为动态 Flask Web 应用，提供类似 WeTravel 的现代化后台管理系统。

## 目录

- [项目简介](#项目简介)
- [技术栈](#技术栈)
- [项目状态](#项目状态)

> 人工文档见 [手册/README.md](../手册/README.md)。功能细节见 [03_功能模块/](03_功能模块/)、进度见 [08_任务追踪.md](08_任务追踪.md)。目录结构见 [01_架构与规范.md](01_架构与规范.md)。

---

## 项目简介

### 项目背景

- **原项目**: 静态 HTML 网站（克隆自 nhtours.com，45个页面）
- **重构目标**: 动态 Flask Web 应用 + WeTravel 风格后台管理系统
- **核心价值**: 支持行程管理、在线预订、Stripe 支付处理及客户管理

### 项目阶段

| 阶段 | 状态 | 说明 |
|------|------|------|
| Phase 1 | ✅ 已完成 | 静态网站 → Flask 动态应用 |
| Phase 2 | 🔄 进行中 | WeTravel 风格后台管理系统 |

---

## 技术栈

### 后端

| 技术 | 用途 |
|------|------|
| Flask | Web 框架 |
| SQLAlchemy | ORM 数据库操作 |
| Flask-WTF | 表单处理和验证 |
| Flask-Login | 用户认证 |
| boto3 | AWS SES 邮件发送 |
| APScheduler | 定时任务（分期付款提醒） |
| Stripe | 支付处理 |

### 前端

| 技术 | 用途 |
|------|------|
| Jinja2 | 模板引擎 |
| Tailwind CSS | CSS 框架 |
| Heroicons | SVG 图标 |
| Quill.js | 富文本编辑器 |
| FullCalendar | 日历视图 |
| Flatpickr | 日期选择器（Trip Builder，Safari/Chrome 一致） |
| Vanilla JS | 原生 JavaScript（无重型框架） |

### 数据库

| 环境 | 数据库 | 说明 |
|------|--------|------|
| 开发 | SQLite 或 **本机 MySQL 8** | 测支付/分期建议用 MySQL；`flask db upgrade` |
| 生产（现网） | MySQL 8 on **VM localhost** | 待迁移 |
| 生产（目标） | **Lightsail 托管 MySQL** | **方案 B**：VM 仅跑应用；见 `08` 迁移清单 |

### 部署

| 技术 | 用途 |
|------|------|
| Gunicorn/Waitress | WSGI 服务器 |
| Nginx | 反向代理、静态文件服务 |
| Docker | 容器化部署（可选） |

---

## 项目状态

### 已完成功能

#### 后台管理

- ✅ 行程管理（My Trips）：创建、编辑、发布行程
- ✅ Trip Builder：7 步向导式行程构建器
- ✅ 日历视图：FullCalendar 集成
- ✅ 客户管理：Customers / Leads 列表
- ✅ 订单管理：Booking 列表（**Order number** + 内部 id）、搜索、筛选、导出 Excel
- ✅ 业务单号：`YYMM{ABBR}-{SEQ}`（如 `2612MT-001`）；创建时生成；改期不改旧号
- ✅ 支付管理：Payment 记录、分期付款管理
- ✅ 退款功能：基础金额退款（手续费不退）；默认不退定金（可选 Also refund deposit）；$0 可仅取消；Stripe + Webhook 幂等；Manual only

#### 前台功能

- ✅ 行程详情页（`/trips/<slug>`，主模板 `booking/trip_booking.html`）
- ✅ 多步骤报名流程（弹窗内：Package → Participant → Add-ons → Payment）
- ✅ Stripe 支付集成（Payment Element）
- ✅ 折扣码系统
- ✅ 分期付款支持
- ✅ **正式首页**（`/`）：Modern V1 布局已上线（Hero、Apart、Testimonials、Programs、Newsletter）
- ✅ **旧版首页存档**（`/home-classic`）：经典 Wukong 叙事布局 + 透明导航，供参考
- ✅ **全站导航**：Modern V1 浅色 navbar（`includes/nav.html` + `site-nav.css`）；旧版见 `includes/legacy/`
- ✅ **Our Team 正式页**（`/our-team`）
- ✅ **Asia Family Photo Gallery**：Photo 1 已修复并上线
- ✅ **生产域名 + HTTPS**：https://nhtours.com 、https://www.nhtours.com（GoDaddy DNS → Lightsail `54.69.40.218`；Let's Encrypt）
- ✅ **Stripe 沙盒 Webhook**：`https://nhtours.com/webhooks/stripe`（Test mode；**Live mode 待收款实测后再配**）
- ✅ **部署方式**：`git push origin main` → GitHub Actions **Deploy** → Lightsail `/var/www/nhtours`（IP 仍可用）
- 🔄 **全站 UI 预览**：Hero 下 indigo 条、面包屑已在 `site-nav.css` **临时隐藏**（待确认后保留或恢复）
- ✅ **全站程序徽章**：`.nh-badge` 与首页 Popular/NEW 样式统一
- ✅ **Post-trip Feedback**：`/feedback`（邮件链接）；与 Testimonials 同表审核，批准后上首页轮播

#### 系统功能

- ✅ 用户认证（Login/Logout）
- ✅ **生产安全加固**（2026-07-06）：审计 JSONL、登录限流、Session 加固；凭据仅 Actions/SSH 轮换；查看 **`nh-audit`**
- ✅ 邮件通知（AWS SES：**生产已配置** domain `nhtours.com` / `us-west-2`，出沙箱；发件如 `noreply@nhtours.com`）
- ✅ 分期付款提醒（定时任务）

### 待完成功能

详见 [08_任务追踪.md](08_任务追踪.md) 待办表（SES 生产配置、PendingBooking 清理、自动化测试等）。

---

**最后更新**: 2026-07-26（业务单号 Order number）

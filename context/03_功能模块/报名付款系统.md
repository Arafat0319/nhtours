# 报名付款系统

本文档详细说明 NH Tours 的预订流程和 Stripe 支付集成。

## 目录

- [系统概述](#系统概述)
- [报名流程](#报名流程)
- [支付流程](#支付流程)
- [Stripe 集成](#stripe-集成)
- [Webhook 处理](#webhook-处理)
- [金额计算逻辑](#金额计算逻辑)
- [测试指南](#测试指南)

---

## 系统概述

### 设计原则

1. **延迟创建**: 正式 Booking 记录在支付成功后才创建
2. **幂等处理**: Webhook 可能重复触发，需防止重复创建
3. **手续费分离**: `amount_paid` 只记录基础金额，不含 Stripe 手续费
4. **业务单号**: 正式 Booking 创建时分配不可变 `order_number`（如 `2612MT-001`）；PendingBooking / 支付中不生成。规则与实现见 `app/order_numbers.py`、`context/AI_快速参考.md`

### 核心状态流转

```
客户开始报名
    ↓
PendingBooking 创建 (status: pending)
    ↓
应付 > 0 → Stripe PaymentIntent；应付 = 0（免定金 code 等）→ free_{uuid}，不调 Stripe
    ↓
客户 Confirm / 完成支付
    ↓
Webhook 或 /api/booking/create-free
    ↓
Booking 创建 (status: deposit_paid / fully_paid) + 分配 order_number
PendingBooking 更新 (status: completed)
Payment 记录创建（卡付为真实 Stripe；`$0` / create-free 也可有本笔 Payment，金额可为 0）
```

---

## 报名流程

### 5 步向导界面

| 步骤 | 页面 | 功能 |
|------|------|------|
| Step 1 | 套餐选择 | 套餐卡 + Travelers ±（`_modal_steps_experimental.html`） |
| Step 2 | 附加选项 | 选择 Add-ons |
| Step 3 | 参与者信息 | 填写每位参与者资料 |
| Step 4 | 购买者信息 | 填写付款人资料 |
| Step 5 | 支付 | Stripe；应付=$0 则 create-free |

### 数据流

```
前端在弹窗内收集数据
    ↓
POST /trips/<slug>（AJAX JSON）
    ↓
后端创建 PendingBooking + Stripe PaymentIntent
    ↓
返回 client_secret
    ↓
POST /api/payment/quote → 算价
POST /api/payment/intent → 更新 PI 金额/metadata（含卡费）
    ↓
前端 Stripe Payment Element 确认支付
    ↓
支付成功 → GET /api/payment/status 轮询
    ↓
Webhook 异步处理 → 创建 Booking / Payment / Installment
```

> **`/api/payment/intent` 不创建 PendingBooking**；首步创建在 `POST /trips/<slug>`。

### 导航栏与脚本

- **主站**：使用 `includes/nav.html`（Logo + Students / Family / Business + Contact Us；移动端完整菜单）。
- **报名页与分期页**：使用 `includes/nav_booking.html`（桌面仅 Logo，移动端 Asia / North America / Mindx）。
- **navigation.js**：根据 `body.booking-layout` 或 `body.installment-modal-page` 分支——主站用原版逻辑（仅 hidden 控制显隐，向上滚动移除 -translate-y-full）；付款页用现有逻辑（hidden + -translate-y-full，初始根据 scrollY 隐藏）。

### 前端模板与 URL 对应

| URL | 主模板 | 说明 |
|-----|--------|------|
| `GET/POST /trips/<slug>` | `booking/trip_booking.html` | 正式页；**`use_experimental_modal=True`** → `_modal_steps_experimental.html` |
| `GET/POST /trips/<slug>/design-preview` | `booking/trip_booking_experimental.html` | 设计预览（同数据 + 横幅）；弹窗同样 experimental |
| `/booking/<id>/receipt` | PDF 默认 / `receipt.html`（`?format=html`） | 客户收据；须 `?token=`；邮件按钮用签名 URL |
| 支付页（邮件链接等） | `booking/payment.html` | 独立支付页，由 `payment.js` 驱动 |
| `/payment/pending?payment_intent_id=...` | `booking/payment_pending.html` | 支付完成后的「处理中」页，轮询 status |
| `/booking/success?booking_id=...` | `booking/success.html` | 预订成功页 |
| `GET /pay-installment/<id>?token=...` | `booking/installment_modal_page.html` | 分期付款弹窗页（邮件链接打开，整页弹窗，左 PAYMENT 右 Your Booking） |
| 分期付款页（其它入口） | `booking/installment_payment.html` | 分期尾款支付，由 `payment.js` 驱动 |

```
templates/
├── includes/
│   ├── nav.html               # 主站导航（Logo + Students / Family / Business + Contact Us）
│   └── nav_booking.html       # 报名/分期页导航（桌面仅 Logo，移动 Asia/NA/Mindx）
templates/booking/
├── base.html                  # 报名/付款布局基座（booking-layout、弹窗时 body 锁定；使用 nav_booking）
├── trip_booking.html          # 行程详情页 + 报名弹窗主体
├── trip_booking_experimental.html  # 设计预览实验页（默认继承 trip_booking，仅加横幅；可替换为新设计代码）
├── _modal_steps_experimental.html  # **现用**步骤 UI（套餐卡 + stepper）
├── _modal_steps.html          # 旧步骤（Uiverse 数量下拉；保留对照）
├── _modal_payment_step_content.html  # 支付步骤内容（报名弹窗与分期页共用）
├── receipt.html               # 收据 HTML 版
├── installment_modal_page.html  # 分期付款弹窗页（继承根 base，block nav → nav_booking，CSS 隐藏 #pageNav）
├── payment.html               # 独立支付页面
├── payment_pending.html       # 支付处理中页面
├── success.html               # 预订成功页面
└── installment_payment.html  # 分期付款页面
```

前端脚本：`booking.js`、`booking-modal.js`、`payment.js`、`installment_modal.js`。细则见 [07](../07_开发日志.md)、[分期付款](分期付款.md)、[支付与后台金额数据流](支付与后台金额数据流.md)。

### Your Booking / 付款后摘要 / 收据

| 项 | 说明 |
|----|------|
| 弹窗 Trip Total | 套餐+附加**目录价**合计 |
| `GET /api/booking/<id>/summary` | 须 `token` 或 `payment_intent_id`；`trip_total` 用目录价；**勿用**本笔 `Payment.base` |
| 折扣 Apply | 须先选套餐；否则 `#discount-message` 琥珀提示（`discount-msg--warn`），不请求 API |
| 字段校验 | email/phone/name/dob/zip：前端 `booking.js` + 后端 `booking_validation.py` |
| 确认邮件 | HTML **Download Receipt**（`?token=`）+ PDF 附件；页脚 `nexus-horizons-email.png` |
| `/booking/<id>/receipt` | 须 `?token=`；默认 PDF（含 **Due at booking**）；`?format=html` 网页；无 token → 404 |

### 报名页布局概要（现行）

- 弹窗 Step 1：卡片式套餐、Travelers ±、有分期时显示 Payment Schedule（`_modal_steps_experimental.html`）
- 右侧：折扣 Apply / Remove（红框按钮）、Trip Total / Fee / Due；**付款结果态**（processing / success / failure）整块折扣 UI 隐藏（含已应用行），`updateOrderSummary` 不得重新打开
- 金额：`format_price` / `formatMoneyAmount`；字体 Source Sans Pro
- 部署后样式「没变」：先 Ctrl+F5
- Pencil 旧对照：`designs/booking-modal-design-spec.md`（可能落后于 experimental）

---

## 支付流程

### 支付方式选择

| 方式 | 说明 | 手续费 |
|------|------|--------|
| 全款支付 | 一次性支付总额 | 按卡类型计算 |
| 定金 + 分期 | 先付定金，后续分期 | 每笔单独计算 |

### 手续费计算

仅信用卡收取手续费，借记卡不收：

| 卡类型 | 费率 |
|--------|------|
| American Express | 3.5% |
| Visa / Mastercard | 2.9% |
| Debit Card | 0% |

```python
def calculate_fee(base_amount_cents, funding, brand):
    """
    计算 Stripe 手续费
    - funding: 'credit' / 'debit'
    - brand: 'amex' / 'visa' / 'mastercard'
    """
    if funding != 'credit':
        return 0
    
    if brand == 'amex':
        rate = 0.035
    else:
        rate = 0.029
    
    return round(base_amount_cents * rate)
```

---

## Stripe 集成

### PaymentIntent 创建

**首步**在 `POST /trips/<slug>`（`handle_booking_submission`）中创建 PendingBooking。

- **应付 > 0**：创建 Stripe PaymentIntent，返回 `client_secret`
- **应付 = 0**（折扣覆盖定金等）：`payment_intent_id = free_{uuid}`，`payment_required=false`，前端不加载 Payment Element，Confirm 走 `/api/booking/create-free`（幂等）

折扣按「现在应付」（定金+逾期+addons）扣减，属真减价（可免定金）。`1–49` 美分视为非法金额直接报错。

**`/api/payment/intent`** 仅更新已有 PI 的金额与 metadata（如卡费），详见 [04_API参考](../04_API参考.md)。取消 PI 统一用 `safe_cancel_payment_intent`（已 canceled 不报错）。

### 自定义问题 File Upload（护照等）

- Builder 题型 `file`；`POST /api/booking/upload` 须 **`trip_id`** + 文件魔数校验 → `static/uploads/booking/trip_<id>/`
- 允许 jpg/png/webp/pdf，≤10MB；落盘必须带合法扩展名（避免 `secure_filename('.jpg')`→`jpg`）
- 答案在 `BookingParticipant.question_answers`：`{type, value: path, original_filename}`

### Payment Element 前端集成

```javascript
// 初始化 Stripe
const stripe = Stripe(STRIPE_PUBLIC_KEY);
const elements = stripe.elements({ clientSecret });

// 挂载 Payment Element
const paymentElement = elements.create('payment');
paymentElement.mount('#payment-element');

// 确认支付
const { error } = await stripe.confirmPayment({
    elements,
    confirmParams: {
        return_url: window.location.origin + '/payment/pending'
    }
});
```

---

## Webhook 处理

### 监听事件

| 事件 | 处理函数 | 说明 |
|------|----------|------|
| `payment_intent.succeeded` | `handle_payment_intent_succeeded` | 支付成功 |
| `checkout.session.completed` | `handle_checkout_completed` | Checkout 完成 |

### payment_intent.succeeded 处理

```python
def handle_payment_intent_succeeded(payment_intent):
    """
    处理 PaymentIntent 成功事件
    
    关键逻辑：
    1. 从 metadata 获取 pending_booking_id
    2. 创建正式 Booking 记录
    3. 创建 Payment 记录（base_amount 不含手续费）
    4. 更新 PendingBooking 状态
    5. 发送确认邮件
    """
    metadata = payment_intent.get('metadata', {})
    pending_id = metadata.get('pending_booking_id')
    
    # 幂等检查
    existing = Payment.query.filter_by(
        stripe_payment_intent_id=payment_intent['id'],
        status='succeeded'
    ).first()
    if existing:
        return  # 已处理，跳过
    
    # 获取 PendingBooking
    pending = PendingBooking.query.get(pending_id)
    booking_data = json.loads(pending.booking_data)
    
    # 创建 Booking
    booking = _create_booking_from_metadata(booking_data, payment_intent)
    
    # 更新 PendingBooking
    pending.status = 'completed'
    pending.booking_id = booking.id
    
    db.session.commit()
```

### 金额字段说明

| 字段 | 位置 | 说明 |
|------|------|------|
| `base_amount_cents` | metadata | 基础金额（不含手续费） |
| `fee_cents` | metadata | 手续费金额 |
| `amount_total` | PaymentIntent | Stripe 收取的总金额 |

**重要**: `Booking.amount_paid` 只记录 `base_amount`，因为手续费是给 Stripe 的，不是我们的收入。

---

## 金额计算逻辑

### 前端 Your Booking 与后端对应关系

报名弹窗右侧「Your Booking」展示与后台一致，规则如下：

| 展示项 | 含义 | 后端对应 |
|--------|------|----------|
| **Trip Total** | 行程全价合计（套餐全价×数量 + 附加项） | `calculate_booking_total` 的 **subtotal** |
| **Due at Booking** | 本次应付：分期时为 deposit×数量 + 逾期分期 + 附加项 − 折扣 + 卡费；全款时为全价 − 折扣 + 卡费 | `calculate_initial_payment_amount` 的 initial_amount − 折扣 + 手续费；有 quote 时用 `lastQuote.final_amount` |
| 逾期分期 | 仅当 `installments[].date`（YYYY-MM-DD）**严格早于当天**计入 | `payments.py` 中 `due_date < today` |

前端在 `updateOrderSummary()` 前通过 `syncBookingDataFromDOM()` 从 DOM 同步套餐/附加项，并按 `package_id` 合并数量，避免重复计费。有 `lastQuote.final_amount` 时 Due at Booking 直接使用后端返回值，保证与后台一致。

---

### calculate_booking_total

```python
def calculate_booking_total(booking):
    """
    返回:
    - subtotal: 套餐 + 附加项
    - discount: 折扣金额
    - total: 净应付金额（subtotal - discount）
    - amount_paid: 已支付金额（不含手续费）
    - amount_due: 待支付金额
    """
    subtotal = 0.0
    
    # 套餐金额
    for bp in booking.booking_packages.all():
        subtotal += bp.package.price * bp.quantity
    
    # 附加项金额
    for addon in booking.addons.all():
        subtotal += addon.addon.price * addon.quantity
    
    # 折扣
    discount = booking.discount_amount or 0.0
    total = max(0.0, subtotal - discount)
    
    amount_paid = booking.amount_paid or 0.0
    amount_due = max(0.0, total - amount_paid)
    
    return {
        'subtotal': subtotal,
        'discount': discount,
        'total': total,
        'amount_paid': amount_paid,
        'amount_due': amount_due
    }
```

### 报表统计

| 指标 | 计算方式 | 说明 |
|------|----------|------|
| `amount_gross` | 套餐 + 附加项 | 原价总额 |
| `amount_discount` | sum(booking.discount_amount) | 折扣总额 |
| `amount_expected` | gross - discount | 应收金额 |
| `amount_paid` | sum(booking.amount_paid) | 已收金额（不含手续费） |
| `amount_available` | expected - paid | 待收金额 |

---

## 测试指南

### 付款失败对客展示

- **文案来源**：优先 Stripe `confirmPayment` 的 `error.message`；轮询失败时用 `/api/payment/status` 的 `error_message`（PI `last_payment_error`）
- **UI**：暖色 callout（`.payment-failure-reason`）+「No charge was made…」；**不**展示原始 `error.code` / JSON
- **Try Again**：回到付款步；样式与收据次要按钮一致

### Stripe 测试卡号

| 卡号 | 类型 | 说明 |
|------|------|------|
| `4242424242424242` | Visa Credit | 成功支付 |
| `4000056655665556` | Visa Debit | 无手续费 |
| `378282246310005` | Amex | 3.5% 手续费 |
| `4000000000009995` | - | 余额不足失败（可验失败原因 UI） |

### 测试流程

1. **新建测试行程**: 设置套餐价格为整数（如 $1000）
2. **完成报名流程**: 使用测试卡支付
3. **验证 Booking**: 检查 `amount_paid` 是否为整数（不含手续费）
4. **验证 Payment**: 检查 `base_amount_cents` 和 `fee_cents`
5. **验证报表**: Reports 页面金额应正确

### 常见问题排查

| 问题 | 原因 | 解决 |
|------|------|------|
| amount_paid 含小数 | 历史数据包含手续费 | 运行数据修复脚本 |
| Webhook 未触发 | Stripe CLI 未运行 | 启动 `stripe listen` |
| 支付页面卡住 | PaymentIntent 失败 | 检查控制台错误 |

---

## 更新日期

**最后更新**: 2026-07-27（Promo 提示；字段校验；收据 Due at booking / logo）

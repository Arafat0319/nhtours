# 任务追踪

本文档记录 NH Tours 项目的功能完成状态和待办事项。

## 目录

- [Phase 1 完成状态](#phase-1-完成状态)
- [Phase 2 功能状态](#phase-2-功能状态)
- [待办事项](#待办事项)
- [已知问题](#已知问题)

---

## Phase 1 完成状态

Phase 1 目标：将静态 HTML 网站重构为 Flask 动态应用。

### 已完成 ✅

| 任务 | 说明 |
|------|------|
| Flask 项目结构 | Application Factory + Blueprints |
| 模板迁移 | 45 个页面迁移到 Jinja2 |
| 模板继承 | base.html + 页面模板 |
| 静态资源组织 | CSS/JS/Images 结构化 |
| Nginx 配置 | 反向代理 + 静态文件服务 |
| SSL 配置 | Let's Encrypt 证书 |
| **生产域名** | ✅ **https://nhtours.com**（GoDaddy DNS → Lightsail，2026-06-30） |
| **生产数据库** | ✅ **方案 B 完成**（Lightsail `nhtours-db`；VM MySQL 已停用） |
| Contact 表单 | Lead 数据存储 |

---

## Phase 2 功能状态

Phase 2 目标：构建 WeTravel 风格的后台管理系统。

### 后台管理 - Admin

| 功能 | 状态 | 说明 |
|------|------|------|
| 仪表盘 | ✅ 完成 | 基础统计展示 |
| 行程列表 | ✅ 完成 | 卡片视图 + 筛选 |
| 日历视图 | ✅ 完成 | FullCalendar 集成 |
| 客户管理 | ✅ 完成 | Customers / Leads（折叠+批量删除）/ Testimonials（批量删除，approved 须确认） |
| 财务报表 | ✅ 完成 | Reports 页面 |
| 数据导出 | ✅ 完成 | 静态 Excel：Refunds / Net Paid / Canceled sheet；WeTravel 式表头 |

### Trip Builder - 行程构建器

| 步骤 | 状态 | 说明 |
|------|------|------|
| Step 1 基础信息 | ✅ 完成 | 标题、日期（Flatpickr）、封面图、日历颜色、**Trip ID**（`trip_abbr`）；URL Slug **Copy** + **QR Code** 下载 |
| Step 2 描述 | ✅ 完成 | Quill 富文本编辑器 |
| Step 3 套餐管理 | ✅ 完成 | 价格、支付计划配置 |
| Step 4 附加选项 | ✅ 完成 | Add-ons 管理 |
| Step 5 购买者信息 | ✅ 完成 | 字段配置 |
| Step 6 参与者问卷 | ✅ 完成 | 自定义问题 |
| Step 7 折扣码 | ✅ 完成 | 创建/管理折扣码 |

### 报名付款 - Booking Flow

| 功能 | 状态 | 说明 |
|------|------|------|
| 套餐选择 | ✅ 完成 | Step 1 |
| 附加选项 | ✅ 完成 | Step 2 |
| 参与者信息 | ✅ 完成 | Step 3 动态表单 |
| 购买者信息 | ✅ 完成 | Step 4 |
| 支付页面 | ✅ 完成 | Step 5 Stripe Payment Element |
| 支付成功页 | ✅ 完成 | 确认信息展示 |
| 折扣码应用 | ✅ 完成 | 验证 + 计算 |

### 支付系统 - Payments

| 功能 | 状态 | 说明 |
|------|------|------|
| Stripe PaymentIntent | ✅ 完成 | 创建/更新 |
| Payment Element | ✅ 完成 | 前端集成 |
| Webhook 处理 | ✅ 完成 | 幂等性保证 |
| 定金支付 | ✅ 完成 | deposit 模式 |
| 分期付款 | ✅ 完成 | installment 记录 |
| Payoff 付清 | ✅ 完成 | 一次性结清 |
| 退款功能 | ✅ 完成 | 按 Payment 手填或部分 Full refund 全渠道原路退；客户 refund notice；卡费不退 |
| 账本告警 | ✅ 完成 | 每日 04:00 扫描 + reconcile API；异常邮件管理员 |
| 手续费计算 | ✅ 完成 | 按卡类型计算 |

### 订单管理 - Booking Management

| 功能 | 状态 | 说明 |
|------|------|------|
| 业务单号 Order number | ✅ 完成 | `YYMM{ABBR}-{SEQ}`；建单时生成；历史已回填；见 `order_numbers.py` |
| 订单列表 | ✅ 完成 | 搜索（含 order_number）、筛选、分页；显示 Order number |
| 订单详情 | ✅ 完成 | 参与者、支付记录 |
| 收据生成 | ✅ 完成 | PDF 定稿见 `receipt-pdf-layout.mdc`；样例 `flask-app/docs/receipt-reference/`；History 按一次付全款 vs 定金/分期 |
| 退款操作 | ✅ 完成 | Manage → Refund：单选笔手填或 Full refund；原因必填；可取消/$0 取消；发客户通知 |

### 邮件通知 - Email

| 功能 | 状态 | 说明 |
|------|------|------|
| AWS SES 集成 | ✅ 完成 | 生产：`nhtours.com` / `us-west-2`，已出沙箱；发件如 `nhtours-noreply@nhtours.com` |
| 支付确认邮件 | ✅ 完成 | HTML + PDF 附件 + Download（签名 `?token=`）；`$0` 同路径 |
| 分期提醒邮件 | ✅ 完成 | HTML+logo；到期前/逾期催款；生产需 `BASE_URL=https://nhtours.com` |
| Contact 表单通知 | ✅ 完成 | Lead 通知 |

### 前台首页改版 - Home Preview

| 任务 | 状态 | 说明 |
|------|------|------|
| 正式首页 Modern V1 | ✅ 完成 | `/`：Hero、Tagline、Apart、Testimonials、Programs、Newsletter |
| 旧版首页存档 | ✅ 完成 | `/home-classic` |
| 全站新导航 | ✅ 完成 | `includes/nav.html` + `site-nav.css` |
| `/home-preview` 兼容重定向 | ✅ 完成 | GET 301 → `/` |
| Programs 卡片徽章 | ✅ 完成 | 全站 `.nh-badge`（Popular `#d59961` / NEW `#004174`） |
| Our Team 正式页 | ✅ 完成 | `/our-team` |
| 全站 indigo 底条 / 面包屑 | 🔄 预览中 | `site-nav.css` 临时 `display: none`；确认后定稿或恢复 |
| Asia Family Photo Gallery | ✅ 完成 | Photo 1 → `photo-1.png` |
| Hubei Photo Gallery 8/11 | ✅ 完成 | `555.B_efXpaN.jpeg`、`5f2d7cda-…BIm6aesN.jpeg` |
| 首页 Testimonials 反馈 | ✅ 完成 | 访客表单 → 审核 → 轮播；**自动垃圾过滤**（蜜罐/限流/规则） |
| Post-trip Feedback 页 | ✅ 完成 | `/feedback` 私有链接；扩展字段 + 同表审核；同样垃圾过滤 |
| Testimonials 后台管理 | ✅ 完成 | Customers → Testimonials；Source/Rating 筛选；弹窗 CRUD；rejected **90 天后自动删** |

---

## 待办事项

### 当前未完成（2026-07-27 晚间核实后）

| 优先级 | 事项 | 状态 | 说明 |
|--------|------|------|------|
| 高 | **数据库方案 B** | ✅ 完成 | 2026-07-27 切库 + 停 VM MySQL + 快照 |
| 高 | **Stripe Live** | ⏳ 故意延后 | 仍 Test mode |
| 低 | 支付页偶发卡顿 | ❓ 未复现 | 仅历史记载 |

**本轮已落地（勿再当待办）**

| 项 | 结论 |
|----|------|
| `auto_billing` Builder 勾选 | ✅ 已从 UI 移除（无后端自动扣款） |
| 团队权限 MVP | ✅ `User.role`（admin/staff）+ `@admin_required`；migration `add_user_role` |
| Pytest 骨架 | ✅ `pytest.ini` + `tests/`（门禁/折扣）；主冒烟仍 `local_tests/` |
| trips 列表金额 N+1 | ✅ `calculate_trip_stats` 批量预取 |
| indigo / 面包屑 | ✅ **正式隐藏**（`site-nav.css` 定稿） |
| `.nh-download-btn` | ✅ `static/css/nh-download-btn.css` |
| MySQL 本机 + `SECURITY_ALERTS` + Lightsail 防火墙 | ✅ 2026-07-27 SSH/`true`/用户核实仅 22·80·443 |
| Pay Off「两条入口」 | ❌ 误报（勾选改金额） |
| SES / APScheduler context | ✅ 已完成 / 已修复 |
| 支付安全加固 Deploy | ✅ `45bfdb8`+本轮；生产探针 403/404 |

### 高优先级（历史进度表）

| 任务 | 说明 | 预计工作量 |
|------|------|-----------|
| 首页实验页合并至 `/` | ✅ 完成 | 2026-06-23 已上线 |
| **GoDaddy 域名 + HTTPS** | ✅ 完成 | 2026-06-30；见 `07_开发日志` |
| **Stripe Webhook（沙盒）** | ✅ 已配置 | Test mode only；**Live mode 延后**至后台收款沙盒测试完成后 |
| 分期付款页面优化 | ✅ 基本完成 | 已有 `installment_modal_page.html` + `installment_payment.html`；后续可按 UI 微调 |
| **AWS SES 生产配置** | ✅ 完成 | domain / DKIM / 出沙箱；排障仍见 `06` SES 节 |
| PendingBooking 自动清理 | ✅ 每天 03:00 将过期 pending → `expired`，并尽量 cancel Stripe PI | 2026-07-21 |
| **deploy/ 脚本与 Deploy** | ✅ 在 `main` | 现网 Deploy 用 **`flask-app/venv`**（与 gunicorn 一致） |
| **数据库迁移（方案 B）** | ✅ 完成 | 切库 + 停 VM MySQL + 快照 `nhtours-db-post-cutover-20260727` |
| **生产安全加固** | ✅ 完成 | 2026-07-06；Security Hardening #5 + 用户验证登录 |
| **支付安全审计修复** | ✅ 完成 | 2026-07-27：`45bfdb8`；折扣服务端重算、summary/receipt 门禁、取消停催 PI、success Download 等 |
| **中低优落地** | ✅ 完成 | 去 auto_billing UI；role MVP；pytest；stats 预取；indigo 定稿；Download CSS |

### 安全上线清单（2026-07-05 → 2026-07-06 生产完成）

> 详见 [手册/安全手册.md](../手册/安全手册.md)。

| 步骤 | 状态 |
|------|------|
| 仓库内代码 + `deploy/security/` | ✅ |
| 本地审计 / 登录限流 / 封锁恢复 | ✅ |
| `git push` → GitHub Actions Deploy | ✅ #20 成功（2026-07-06） |
| Actions → **Security Hardening** | ✅ #5 成功 |
| 浏览器验证新凭据 + 旧账号失效 | ✅ 2026-07-06 |
| 审计日志写入 + 权限 `ubuntu:www-data` | ✅ 2026-07-06 |
| **`nh-audit` 生产可用** | ✅ 2026-07-06（`201dc72` 修复 symlink） |
| Deploy 自动 `setup_audit_log.sh` | ✅ `67da10c` |
| MySQL 仅 localhost | ✅ 2026-07-27 SSH：`bind-address=127.0.0.1`，3306 仅本机监听 |
| SES 后 `SECURITY_ALERTS_ENABLED=true` | ✅ 2026-07-27 已改为 `true` 并 `restart nhtours` |
| Lightsail 网络防火墙仅 22/80/443 | ✅ 2026-07-27 用户控制台核实：仅 22/80/443 |

**已完成（仓库内 + 生产）**：审计模块、登录限流、Session Cookie、export token 90 天、`deploy/security/` 脚本、**`nh-audit`**；**凭据轮换** 仅 Actions/SSH（无网页改密）。

---

### 📋 数据库迁移（方案 B）— 与用户分步实施

> **提醒**：开始创建托管库、改 `DATABASE_URL` 或停 VM 本机 MySQL 之前，先读本节。Stripe **Live** 仍延后；迁移可在沙盒收款测试期间或之后进行。

**目标架构**

```
用户 → https://nhtours.com → Lightsail VM（Nginx + Gunicorn）
                                    ↓ DATABASE_URL
                          Lightsail 托管 MySQL（主库 + AWS 自动备份）
```

**现网 → 目标**

| 项 | 现网 | 迁移后 |
|----|------|--------|
| MySQL | VM 本机 `localhost` | Lightsail **托管 MySQL** |
| 开发 | 本机 SQLite 或 MySQL | 建议本机 **MySQL 8**（`nhtours_dev`）；支付/分期用 MySQL 测 |
| 数据安全 | 依赖 VM 磁盘 | AWS 自动备份 + 应用与数据分离 |

**实施清单（按顺序，与用户协作）**

1. [x] Lightsail → 创建 `nhtours-db` MySQL 8（`us-west-2a`，`micro_2_0`）
2. [x] endpoint / 主用户；**Public mode 关闭**（仅同账号 Lightsail 可连）
3. [x] VM：`mysqldump` → 导入托管库
4. [x] 改 **`/var/www/nhtours/flask-app/.env`** 的 `DATABASE_URL` → 托管 endpoint
5. [x] `systemctl restart nhtours`（schema 已随 dump 一致）
6. [x] **冒烟**：用户确认后台/数据正常（2026-07-27）
7. [x] VM **停用** `mysql`（`stop` + `disable`；未卸载，便于紧急回装）
8. [x] 文档 `06` / `07` / `08` / `AI_快速参考` 已记 endpoint（**无密码**）；已打手动快照 `nhtours-db-post-cutover-20260727`

**开发环境说明（与 B 不冲突）**

- 生产 VM **不需要** MySQL；开发机 **可以且建议** 本机 MySQL 或 Docker MySQL
- 示例：`DATABASE_URL=mysql+pymysql://user:pass@127.0.0.1:3306/nhtours`（本地；可整库复制生产，见 `AI_快速参考` / `07` 2026-07-26）
- 生产托管迁移完成后：本地仍可用本机库；生产用托管 endpoint

**未选方案（备忘）**

- ~~VM 主库 + AWS 从库实时复制~~：运维复杂，本项目不采用
- ~~仅 cron dump 到 S3~~：可作迁移后 **补充** 备份，不替代托管库

**相关文档**：`06_部署指南` →「生产数据库（方案 B）」

---

### 📧 AWS SES（生产已完成 — 旧勾选清单归档）

> **2026-07-27**：SES 生产已配置并在用（收据 / 催款 / Contact 等）。下列为域名迁移期历史清单，**勿再当作「未配 SES」**。排障见 `06` SES 节。

<details>
<summary>历史配置清单（已完成，仅备查）</summary>

1. [x] AWS SES 验证域名 `nhtours.com`
2. [x] DKIM CNAME → GoDaddy DNS
3. [x] 移出 SES 沙箱
4. [x] 生产 `.env`：`AWS_*` / `SENDER_EMAIL` / `RECIPIENT_EMAIL` / `BASE_URL=https://nhtours.com`
5. [x] 冒烟：Contact / 收据邮件 / 分期提醒链接

</details>

**运维注意**

- 代码读 **`SENDER_EMAIL`**（不是 `MAIL_SENDER`）
- `BASE_URL` 须在生产 `.env`（`tasks.py` 催款链接依赖）
- ✅ `SECURITY_ALERTS_ENABLED=true`（2026-07-27 生产已开）

---

### 中优先级

| 任务 | 说明 | 预计工作量 |
|------|------|-----------|
| 团队权限管理 | ✅ MVP：`admin`/`staff` + `@admin_required`；完整 RBAC / 用户管理 UI 仍可选 | — |
| 自动化测试 | ✅ 初建 `pytest` + `tests/`；扩展用例 / 替代 `local_tests` 仍可选 | — |
| 性能优化 | ✅ 本轮：trips stats 批量预取；更大范围缓存仍可选 | — |
| ~~`auto_billing`~~ | ✅ UI 已移除 | — |

### 低优先级

| 任务 | 说明 | 预计工作量 |
|------|------|-----------|
| 多语言支持 | i18n 国际化 | 大 |
| API 文档 | Swagger/OpenAPI | 中 |
| 移动端优化 | 响应式改进 | 中 |
| ~~`.nh-download-btn` 公共 CSS~~ | ✅ `static/css/nh-download-btn.css` | — |
| ~~indigo / 面包屑定稿~~ | ✅ 正式隐藏 | — |

---

## 已知问题

### 需要修复

| 问题 | 严重程度 | 状态 |
|------|----------|------|
| 历史数据手续费问题 | 中 | ✅ 已修复 |
| Safari / Chrome 尾部 `/` 导致页面 404 | 中 | ✅ 已修复；push 后 Deploy 验证（2026-07-10） |
| 支付页面偶发卡顿 | 低 | ❓ 待复现（2026-07-27 未实测） |
| ~~APScheduler `Working outside of application context`~~ | 低 | ✅ **已作废**：job 均 `app.app_context()`（2026-07-27 核实） |

### 技术债务

| 问题 | 说明 |
|------|------|
| ~~`enable_auto_billing` UI~~ | ✅ Builder 已移除该勾选 |
| JavaScript 代码组织 | 部分逻辑需要模块化 |
| 表单验证重复 | 前后端验证需统一 |
| 错误处理不完整 | 部分 API 缺少详细错误信息 |

---

## 里程碑

### Phase 1 ✅ 已完成
- 静态网站迁移
- Flask 基础架构
- 部署配置

### Phase 2 ✅ 核心功能完成
- Trip Builder
- Booking Flow
- 支付系统
- 后台管理

### Phase 3 📋 规划中
- **Lightsail 托管 MySQL（方案 B）**
- 团队协作功能
- Stripe Live
- 高级报表
- 移动端优化

---

## 更新日期

**最后更新**: 2026-07-27（Leads/Testimonials 批量删除；安全核对齐；每次 push 须同步 context）

# 部署指南

本文档详细说明 NH Tours 项目的部署流程和服务器配置。

## 目录

- [环境准备](#环境准备)
- [部署方式](#部署方式)
- [GitHub Actions 自动部署](#github-actions-自动部署仅部署无-ci)
- [生产安全加固（Deploy 后 SSH）](#生产安全加固deploy-后-ssh)
- [Nginx 配置](#nginx-配置)
- [环境变量](#环境变量)
- [Excel 导出刷新功能](#excel-导出刷新功能)
- [故障排查](#故障排查)

---

## 环境准备

### 系统要求

| 组件 | 最低版本 |
|------|----------|
| Python | 3.9+ |
| Node.js | 16+ (构建 Tailwind) |
| Nginx | 1.18+ |
| MySQL | 8.0+ (生产环境) |

### 依赖安装

```bash
# 克隆项目
git clone <repo-url>
cd nhtours-website

# 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Linux/Mac
# 或
.\venv\Scripts\activate   # Windows

# 安装依赖
pip install -r requirements.txt

# 安装前端依赖（如果需要构建 CSS）
npm install
npm run build:css
```

---

## 部署方式

### 方式一：传统服务器部署

#### 1. 配置环境变量

```bash
# 创建 .env 文件
cp .env.example .env

# 编辑配置
nano .env
```

#### 2. 数据库迁移

```bash
cd flask-app
flask db upgrade
```

> **2026-06-24**：首页 Testimonials 依赖 `testimonials` 表；GitHub Actions Deploy 在重启前执行 `flask db upgrade`。
>
> **Deploy 脚本要点**（`.github/workflows/deploy.yml`）：
> - 目录 `/var/www/nhtours`；**venv 必须与 systemd 一致**：`/var/www/nhtours/flask-app/venv`（gunicorn 用此路径；勿再用仓库根 `venv`）
> - `git reset --hard` **前**对 `flask-app/app` / `context` 等 `chown` 为 Deploy 用户（避免热修留下的 `www-data` 导致 unlink 失败）
> - 一律 `$VENV/bin/python -m pip` / `-m flask db upgrade`（勿依赖系统 `flask` 或 `~/.local`）
> - 优先 `source flask-app/.env`，否则回落仓库根 `.env`；再迁移
> - 依赖含 `PyMySQL`、`cryptography`
> - 收据 PDF：`fpdf2==2.8.3` **force-reinstall** 后 `from fpdf import FPDF` + `receipt_pdf` import；失败则 Deploy 失败（避免静默回退 HTML）

#### 3. 启动应用

**开发环境**:
```bash
python run.py
```

**生产环境（使用 Gunicorn）**:
```bash
gunicorn -w 4 -b 0.0.0.0:8000 "app:create_app()"
```

#### 4. 配置 Systemd 服务

```ini
# /etc/systemd/system/nhtours.service（与现网一致；勿再用仓库根 venv 或 :8000）
[Unit]
Description=Gunicorn instance to serve nhtours
After=network.target

[Service]
User=ubuntu
Group=www-data
WorkingDirectory=/var/www/nhtours/flask-app
Environment="PATH=/var/www/nhtours/flask-app/venv/bin"
# 环境变量由 Deploy / 进程环境加载；生产密钥在 flask-app/.env
ExecStart=/var/www/nhtours/flask-app/venv/bin/gunicorn --workers 3 --bind unix:nhtours.sock -m 007 wsgi:app
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
# 启用服务
sudo systemctl daemon-reload
sudo systemctl enable nhtours
sudo systemctl start nhtours
```

### GitHub Actions 自动部署（仅部署，无 CI）

本地自测通过后，推送到 `main` 分支即可自动部署到已有服务器（不跑 CI 测试）。

1. **仓库**：代码已在 GitHub（如 `Arafat0319/nhtours`），并已配置好服务器（方式一：克隆在 `/var/www/nhtours`、systemd 服务名 `nhtours`）。
2. **Workflow**：根目录 `.github/workflows/deploy.yml`，触发条件为 `push` → `main`；SSH 执行 reset 前 chown、`git reset --hard`、装依赖到 **`flask-app/venv`**、优先 `source flask-app/.env`、`flask db upgrade`、校验 fpdf2、重启 `nhtours`。
3. **GitHub Secrets**（Settings → Secrets and variables → Actions）需配置：
   | Secret | 说明 |
   |--------|------|
   | `DEPLOY_HOST` | 服务器 IP 或域名 |
   | `DEPLOY_USER` | SSH 登录用户（如 `ubuntu`） |
   | `DEPLOY_SSH_KEY` | 部署用 SSH 私钥全文（服务器上需已加入对应用户的 `authorized_keys`） |
4. **部署目录**：workflow 中默认为 `/var/www/nhtours`；若不同，请编辑 `.github/workflows/deploy.yml` 中 `cd` 后的路径。
5. **首次**：在服务器上已克隆本仓库并配好 venv、systemd 后，之后只需 `git push origin main`，Actions 会自动完成拉取与重启。

#### 如何查看 Deploy 是否成功

1. 打开仓库 **Actions** 页：`https://github.com/Arafat0319/nhtours/actions`
2. 左侧点 **Deploy** workflow
3. 查看最新一次运行：绿色 ✓ = 成功，红色 ✗ = 失败
4. 点进运行 → **Deploy via SSH** 可查看服务器执行日志

#### 日常发布（每次改代码后）

> **流程约定**：先在本地完成测试（CI 在本地，非 GitHub Actions）。测试通过并**确认可以上线**后，再 push；AI 助手默认**不会**未经确认自动 push。

```bash
# 1. 本地自测（必做）
cd flask-app
python run.py          # 默认 http://localhost:8080
# 按改动逐项验证；有 DB 迁移时先 flask db upgrade

# 2. 确认无误后再提交并推送
git status          # 确认代码 + context/ 均已修改并 staged
git add ...
git commit -m "..."
git push origin main
# 然后到 Actions 页确认 Deploy 成功
```

**Context 同步**：push 前按 [11_推送与上下文同步.md](11_推送与上下文同步.md) 更新文档（至少 `07_开发日志.md`），与代码一并 commit。

**生产服务器**：**https://nhtours.com**（Lightsail `54.69.40.218`）；部署目录默认 `/var/www/nhtours`。

> **安全加固（2026-07-06）**：核心项已完成（凭据轮换、审计、限流、`nh-audit`）。**Deploy** 会自动执行 `setup_audit_log.sh` 安装 `nh-audit`；首次或重装后仍可用 SSH 手动跑加固脚本 → 见下节。

### 生产安全加固（Deploy 后 SSH）

> 完整说明：[手册/安全手册.md](../手册/安全手册.md)。凭据勿写入 Git；见本地 `手册/安全手册-凭据.local.md`。  
> **状态（2026-07-06）**：✅ 已上线；日常查审计用 **`nh-audit`**。

**顺序**：① Actions Deploy 绿 → ② 改生产 `.env` → ③ rotate 凭据 → ④ 审计日志 → ⑤ Nginx → ⑥ fail2ban → ⑦ 浏览器验证。

#### 1. 确认 Deploy 成功

GitHub → **Actions** → **Deploy** → 最新运行绿色 ✓。

#### 2. 生产 `.env` 追加（Lightsail SSH）

```bash
sudo nano /var/www/nhtours/flask-app/.env
```

追加（SES 未就绪前告警仅写日志）：

```bash
SECURITY_AUDIT_LOG=/var/log/nhtours/audit.log
SECURITY_ALERTS_ENABLED=false
# SECURITY_ALERT_EMAIL=info@nhtours.com   # SES 就绪后改为 true 并取消注释
```

#### 3. 轮换管理员凭据（一次性）

凭据从本地 `手册/安全手册-凭据.local.md` 读取，**勿粘贴到聊天或 commit**：

```bash
cd /var/www/nhtours/flask-app
set -a && source .env && set +a
export FLASK_ENV=production
export NEW_ADMIN_USERNAME='（你的新用户名）'
export NEW_ADMIN_PASSWORD='（你的长密码）'
export OLD_ADMIN_USERNAME='admin'
/var/www/nhtours/flask-app/venv/bin/python scripts/rotate_admin_credentials.py
```

#### 4. 审计日志目录 + nh-audit

Deploy 工作流会在 `flask db upgrade` 后自动执行 `setup_audit_log.sh`（创建目录、**`ubuntu:www-data`** 权限、安装 `/usr/local/bin/nh-audit`）。也可手动：

```bash
sudo bash /var/www/nhtours/deploy/security/setup_audit_log.sh
sudo cp /var/www/nhtours/deploy/security/logrotate-nhtours-audit /etc/logrotate.d/nhtours-audit
sudo systemctl restart nhtours
sudo journalctl -u nhtours -n 30 --no-pager

# 查看（列对齐）
nh-audit
nh-audit -n 50
nh-audit -f
```

#### 5. Nginx 安全头（HTTPS server 块内）

编辑 `/etc/nginx/sites-enabled/nhtours`，在 **443** 的 `server { }` 内、`location` 之前添加：

```nginx
include /var/www/nhtours/deploy/security/nginx-security-headers.conf;
```

可选：在 `/admin/login` 的 `location` 内参考 `deploy/security/nginx-admin-rate-limit.snippet` 加 `limit_req`。

```bash
sudo nginx -t && sudo systemctl reload nginx
```

#### 6. fail2ban（sshd）

```bash
sudo apt-get update && sudo apt-get install -y fail2ban
sudo cp /var/www/nhtours/deploy/security/fail2ban-jail.local.example /etc/fail2ban/jail.d/nhtours.local
sudo systemctl enable fail2ban && sudo systemctl restart fail2ban
sudo fail2ban-client status sshd
```

#### 7. 验证

| 检查 | 命令 / 操作 |
|------|-------------|
| 旧账号失效 | 浏览器：`admin` / `admin123` 应失败 |
| 新账号登录 | https://nhtours.com/admin/login |
| 审计写入 | `nh-audit` 或 `sudo tail -5 /var/log/nhtours/audit.log` → `admin_login_success` |
| 防火墙 | Lightsail 网络：仅 **22、80、443** |
| MySQL | `bind-address = 127.0.0.1`（方案 B 迁移前） |

#### Push 前注意（凭据）

- `手册/安全手册.md` 含明文密码时 **勿 push 到可公开仓库**；可只提交无密码版，或整目录保持本地。
- 已 gitignore：`手册/安全手册-凭据.local.md`。

---

#### 部署后页面未变化的常见原因

| 原因 | 处理 |
|------|------|
| 改动未 commit / push | `git status` 检查；静态图片常见遗漏 |
| Actions Deploy 失败 | Actions 页查看日志；常见：`flask: command not found`（需 venv + `python -m flask`）、缺 `pymysql`、未 `source .env` |
| 浏览器或 Nginx 缓存 | Ctrl+F5；`/static/` 配置了 `expires 30d` |
| 只改了本地未 push 的实验页 | 确认 push 的 commit 含目标文件 |

**案例（2026-06-14）**：`/asia/family` Photo 1 本地有图、线上无图——因图片文件未进 Git；见 `07_开发日志.md` 对应条目。

> **注意**：`flask-app/.github/workflows/deploy.yml` 为旧 Elastic Beanstalk 流程，**非现网 Deploy**。现网仅用仓库根目录 `.github/workflows/deploy.yml`（SSH → Lightsail）。

### 方式二 / 三：Docker、Heroku（未用于现网，略）

现网仅 **方式一 + 根目录 GitHub Actions Deploy**。旧 Docker/Heroku 示例已从本文移除以减冗；需要时可查 Git 历史。

### GoDaddy 域名接入 Lightsail（已完成 2026-06-30）

| 项 | 值 |
|----|------|
| 域名 | `nhtours.com`、`www.nhtours.com` |
| DNS | GoDaddy 默认 NS；`@` **A → 54.69.40.218**；`www` CNAME → `nhtours.com` |
| 静态 IP | 见下节「Lightsail 静态 IP」 |
| HTTPS | Certbot + Let's Encrypt（自动续期） |
| 环境变量 | **`/var/www/nhtours/flask-app/.env`** |
| Gunicorn | `unix:nhtours.sock`（WorkingDirectory=`flask-app`） |

### Lightsail 静态 IP（联网）

| Lightsail 名称 | IP | 状态 | 说明 |
|----------------|-----|------|------|
| **StaticIp-1** | `54.69.40.218` | 已附加到实例 **NHtours** | **现网唯一在用**：DNS / SSH / HTTPS 均指向此地址；**勿删** |
| ~~NHtours~~（资源名） | `52.43.165.50` | 曾「未附加」 | 闲置静态 IP 会按小时计费；**可删**（项目无引用）。删时确认 IP 是 `52.43.…`，勿误删 `StaticIp-1` |

> 控制台路径：Lightsail → **联网** → 静态 IP。未附加的静态 IP 与实例同名时容易混淆，以 **IP 与「已附加」状态** 为准。

**从旧 CloudFront 迁移要点**：Nameserver 从 AWS 改回 GoDaddy；删除 CloudFront A/CNAME；改 `@` A 记录。

| 工具 | 说明 |
|------|------|
| `deploy/verify-dns.ps1` | 本地验证 DNS 是否指向 Lightsail |
| `deploy/setup-domain.sh` | 服务器 Nginx + 可选 `--certbot` |
| GitHub **Setup Domain** | `.github/workflows/setup-domain.yml` 手动触发 |
| Stripe 清单 | [deploy/post-domain-checklist.md](../deploy/post-domain-checklist.md) |

详细步骤：[手册/域名接入GoDaddy.md](../手册/域名接入GoDaddy.md)

### 生产数据库（方案 B — ✅ 已切库 2026-07-27）

> **决策 2026-06-30 / 切库 2026-07-27**：Lightsail 托管 MySQL 为主库；网站 VM 跑 Nginx + Gunicorn。VM 本机 MySQL 已 **stop + disable**（未卸载）。

| 项 | 值 |
|----|------|
| 名称 | `nhtours-db` |
| 引擎 | MySQL 8.0（blueprint `mysql_8_0`，bundle `micro_2_0`） |
| 区域 | `us-west-2a`（与网站实例同区） |
| Endpoint | `ls-47af4f8f5a736ebf459dc24214e234e37f11f6f7.cb86sa6ugljb.us-west-2.rds.amazonaws.com:3306` |
| 库名 | `nhtours` |
| 公网 | **关闭**（`publiclyAccessible=false`） |
| 备份 | 自动备份已开（约 7 天 PITR）；可另打手动快照 |
| 应用连接 | `/var/www/nhtours/flask-app/.env` → `DATABASE_URL` 指向上述 endpoint |
| VM 本机 MySQL | 已 **stop + disable**（未卸载）；回滚见下 |
| 手动快照 | `nhtours-db-post-cutover-20260727` |
| 开发 | **不变**：本机 MySQL；按需从生产 dump（SSH 到网站 VM 再 dump 托管库） |

**回滚（紧急）**：先 `sudo systemctl enable --now mysql`，再把 `.env` 恢复为 `.env.bak.pre-managed-*`（`localhost`）并 `systemctl restart nhtours`。

---

## Nginx 配置

### 基础配置

```nginx
# /etc/nginx/sites-available/nhtours
server {
    listen 80;
    server_name nhtours.com www.nhtours.com;
    
    # HTTP 重定向到 HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name nhtours.com www.nhtours.com;

    # SSL 证书
    ssl_certificate /etc/letsencrypt/live/nhtours.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/nhtours.com/privkey.pem;
    
    # SSL 优化
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
    ssl_prefer_server_ciphers off;

    # 静态文件
    location /static/ {
        alias /var/www/nhtours/flask-app/app/static/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 上传文件
    location /uploads/ {
        alias /var/www/nhtours/flask-app/app/uploads/;
        expires 7d;
    }

    # 反向代理到 Flask
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket 支持（如果需要）
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml;
    gzip_min_length 1000;
}
```

### 启用站点

```bash
sudo ln -s /etc/nginx/sites-available/nhtours /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

### SSL 证书（Let's Encrypt）

```bash
# 安装 Certbot
sudo apt install certbot python3-certbot-nginx

# 获取证书
sudo certbot --nginx -d nhtours.com -d www.nhtours.com

# 自动续期
sudo certbot renew --dry-run
```

---

## 环境变量

### 必需变量

| 变量 | 说明 | 示例 |
|------|------|------|
| `SECRET_KEY` | Flask 密钥 | 随机字符串 |
| `DATABASE_URL` | 数据库连接 | 现网：`...@localhost/...`；方案 B：`...@<lightsail-db-endpoint>/...` |
| `STRIPE_SECRET_KEY` | Stripe 密钥 | `sk_live_xxx` |
| `STRIPE_PUBLIC_KEY` | Stripe 公钥 | `pk_live_xxx` |
| `STRIPE_WEBHOOK_SECRET` | Webhook 签名 | `whsec_xxx` |

### 可选变量

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `FLASK_ENV` | 运行环境 | `production` |
| `AWS_ACCESS_KEY_ID` | AWS 密钥（SES） | - |
| `AWS_SECRET_ACCESS_KEY` | AWS 密钥（SES） | - |
| `AWS_REGION` | AWS 区域（SES 客户端） | - |
| `SENDER_EMAIL` | 发件邮箱（须在 SES 验证） | `noreply@nhtours.com` |
| `RECIPIENT_EMAIL` | Contact/Feedback 通知收件 | `info@nhtours.com` |
| `BASE_URL` | 邮件内绝对链接基址（分期支付等） | 生产应设 `https://nhtours.com` |
| `EXCEL_REFRESH_BASE_URL` | ~~Excel Web 连接基址~~（已废弃） | — |
| `SECURITY_AUDIT_LOG` | 安全审计 JSONL 路径 | 生产：`/var/log/nhtours/audit.log` |
| `SECURITY_ALERTS_ENABLED` | 是否发安全告警邮件 | 生产 **已为 `true`**（2026-07-27 SSH）；本地开发可仍为 `false` |
| `SECURITY_ALERT_EMAIL` | 安全告警收件 | 默认同 `RECIPIENT_EMAIL` |
| `EXPORT_TOKEN_MAX_AGE_SECONDS` | Excel 导出 token 有效期（秒） | 默认 7776000（90 天） |

### AWS SES（邮件）

> **SES**：生产已配置（domain / 出沙箱）。催款链接依赖 `BASE_URL`。排障见本节；旧「上线前勾选清单」已归档于 `08`。

| 步骤 | 操作 |
|------|------|
| 1 | SES 控制台 → 验证域名 `nhtours.com` |
| 2 | GoDaddy DNS 添加 SES 提供的 **DKIM CNAME**（3 条，与网站 A 记录并存） |
| 3 | 申请移出 SES 沙箱（否则只能发到已验证邮箱） |
| 4 | `flask-app/.env` 配置 `AWS_*`、`SENDER_EMAIL`、`BASE_URL=https://nhtours.com` |
| 5 | `sudo systemctl restart nhtours` → Contact / 支付收据 / 分期提醒 冒烟测试 |

**受影响邮件**：支付确认收据、分期到期/逾期提醒、**管理员通知**（Contact / Newsletter / Testimonial / Feedback / Security，壳 `admin_notify_base.html`）、后台群发。

```bash
# AWS SES（生产 .env 示例片段）
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=AKIAXXXXXXXX
AWS_SECRET_ACCESS_KEY=xxxxxxxxxxxxxxxx
SENDER_EMAIL=noreply@nhtours.com
RECIPIENT_EMAIL=info@nhtours.com
BASE_URL=https://nhtours.com
EXCEL_REFRESH_BASE_URL=https://nhtours.com
```

### .env 文件示例（Stripe 等）

```bash
# Flask
SECRET_KEY=your-very-long-random-secret-key
FLASK_ENV=production

# Database
DATABASE_URL=mysql+pymysql://nhtours:password@localhost/nhtours

# Stripe（当前仅 Test mode；Live 待收款实测后再换 sk_live_/pk_live_）
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxx
STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxx

# AWS SES — 见上文「AWS SES（邮件）」小节；测邮件前必读 08 清单
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=AKIAXXXXXXXX
AWS_SECRET_ACCESS_KEY=xxxxxxxxxxxxxxxx
SENDER_EMAIL=noreply@nhtours.com
RECIPIENT_EMAIL=info@nhtours.com
BASE_URL=https://nhtours.com
EXCEL_REFRESH_BASE_URL=https://nhtours.com

# 安全（SES 就绪后再开告警邮件）
SECURITY_AUDIT_LOG=/var/log/nhtours/audit.log
SECURITY_ALERTS_ENABLED=false
# SECURITY_ALERT_EMAIL=info@nhtours.com
```

---

## Excel 导出刷新功能（⚠️ 已废弃）

> **2026-07 起**：Manage → **Download Excel** 为**静态快照**（打开即有数据），**不再**内置 Web 连接 / Power Query 刷新。下列 `EXCEL_REFRESH_*` / `export/csv?token=` 说明仅作历史参考；现网勿再按此配置。

导出的 Excel ~~内置 Web 连接~~（旧方案）。云端部署时旧方案曾注意以下事项。

### 部署要求

| 项目 | 说明 |
|------|------|
| **导出位置** | 必须在**线上环境**导出 Excel（如 `https://yoursite.com`）。本地 localhost 导出时 URL 会指向 localhost，他人无法刷新。 |
| **反向代理** | 使用 Nginx 等代理时，必须正确传递 `Host` 和 `X-Forwarded-Proto`，否则生成的 URL 可能错误。 |
| **公网可达** | 客户电脑需能访问你的域名，Excel 才能向服务器发起刷新请求。 |

### Nginx 配置（必含）

```nginx
# 反向代理到 Flask 时必须包含以下 header
proxy_set_header Host $host;
proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $scheme;   # 关键：让 Flask 知道是 https
```

### 可选 Flask 配置

若代理配置有误，可在 `config.py` 中显式指定：

```python
# 仅在代理 header 缺失时使用
app.config['PREFERRED_URL_SCHEME'] = 'https'
```

### 部署后自测步骤

1. 登录线上后台 → 行程管理 → 某行程 → **Download Excel** 导出
2. 用 Excel 打开下载的 xlsx 文件
3. 观察打开时是否自动刷新，或右键数据区域 → **刷新**
4. 若刷新失败，在浏览器中访问：  
   `https://yoursite.com/admin/trips/bookings/export/csv?token=xxx&format=html`  
   （token 可在导出时从网络请求中查看）
   - 能返回 HTML 表格：Excel 或网络问题
   - 返回 403/404：域名或代理配置问题

### 常见问题

- **打开不刷新**：检查客户网络是否能访问你的域名；Excel 需联网
- **URL 指向错误**：检查 Nginx 是否配置 `X-Forwarded-Proto`、`Host`

---

## 故障排查

### 常见问题

#### 1. MySQL 认证错误：cryptography package is required

**现象**: 连接 MySQL 时出现 `RuntimeError: 'cryptography' package is required for sha256_password or caching_sha2_password auth methods`。

**原因**: MySQL 8 默认使用 `caching_sha2_password`，PyMySQL 需要 `cryptography` 进行加密认证。

**解决**: `pip install cryptography`

#### 2. 应用无法启动

```bash
# 检查日志
sudo journalctl -u nhtours -f

# 检查端口占用
sudo netstat -tlnp | grep 8000
```

#### 3. 静态文件 404

```bash
# 检查文件权限
ls -la /var/www/nhtours/flask-app/app/static/

# 检查 Nginx 配置
sudo nginx -t
```

#### 4. 数据库连接失败

```bash
# 测试连接
mysql -h localhost -u nhtours -p nhtours

# 检查 MySQL 状态
sudo systemctl status mysql
```

#### 5. Stripe Webhook 失败

```bash
# 检查签名
# 确保 STRIPE_WEBHOOK_SECRET 正确

# 本地测试
stripe listen --forward-to localhost:8080/webhooks/stripe
```

### 日志位置

| 服务 | 日志路径 |
|------|----------|
| Flask | `journalctl -u nhtours` |
| **安全审计** | **`/var/log/nhtours/audit.log`**（JSONL）；查看 **`nh-audit`** / `nh-audit -f`；本地 `flask-app/instance/audit.log` |
| Nginx | `/var/log/nginx/error.log`、`access.log` |
| MySQL | `/var/log/mysql/error.log` |

### 性能优化

1. **Gunicorn Workers**: 设置为 CPU 核心数 × 2 + 1
2. **静态文件缓存**: Nginx 设置 `expires 30d`
3. **数据库连接池**: SQLAlchemy 配置 `pool_size`
4. **Gzip 压缩**: Nginx 启用 gzip

---

## 部署检查清单

- [ ] 环境变量已配置
- [ ] 数据库已迁移（schema：`flask db upgrade`）
- [ ] **（方案 B 完成后）** 生产 `DATABASE_URL` 指向 Lightsail 托管 MySQL；VM 本机 MySQL 已停用
- [ ] SSL 证书已安装
- [ ] Nginx 配置已测试（含 `X-Forwarded-Proto`）
- [ ] Stripe Webhook 已配置
- [ ] 静态文件可访问
- [ ] 日志正常记录
- [ ] 自动重启已配置
- [ ] Excel 导出：在线上导出 xlsx，打开后验证刷新功能
- [ ] `git push` 后 GitHub Actions **Deploy** 为绿色 ✓
- [x] **（安全加固）** 凭据轮换、审计目录、`nh-audit`（2026-07-06）
- [x] **（安全加固）** 生产登录验证 + `audit.log` 有 `admin_login_success`
- [ ] **（安全加固，可选）** Nginx 安全头、fail2ban（见下节步骤 5–6）
- [ ] 新增静态图片已 commit（`git status` 无相关 `??`）；例：Hubei Photo 8/11、北京 `beijing-hero.DEYdmVgd.jpg`
- [ ] 线上关键页验收（如 `/our-team`、`/asia/family` Photo Gallery）

---

## 更新日期

**最后更新**: 2026-07-27（Deploy reset 前 chown；flask-app/venv + fpdf2）

# NH Tours 项目规则

## 部署：本地测试后再 push（必须遵守）

本项目的 CI 在**本地**进行；**未经用户明确确认，不得 `git push origin main`**（会触发 GitHub Actions 自动部署）。

- 改版完成后：给出本地验证步骤，请用户自测。
- 仅在用户确认测试无误、同意推送后，才 push / 部署。
- 详细规则见 **`.cursor/rules/local-test-before-push.mdc`** 与 **`context/01_架构与规范.md`**（开发协作规则）。

## 防灾：防止工作丢失（必须遵守）

在执行任何**会覆盖工作区**的 Git 命令之前，必须先阅读并遵守 **context/10_防灾与备份机制.md**。

- **禁止**在未向用户说明风险、未确认用户已备份或已提交的情况下，执行：
  - `git checkout <commit> -- <文件或路径>`
  - `git restore --source=<commit> <文件或路径>`
  - 其他会用版本库内容覆盖工作区、导致未提交修改丢失的操作。
- 用户要求「回退」「恢复到之前」时：先澄清是「撤销未提交编辑」还是「从版本库恢复」；若为后者，须提示未提交修改会丢失，并建议先备份或先提交再执行。
- 详细流程与自检清单见 **context/10_防灾与备份机制.md**。

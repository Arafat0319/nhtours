# NH Tours 项目规则

- **人工文档**：`手册/` · **AI 文档**：`context/`（先读 `AI_快速参考.md`）
- **未经用户同意不得** `git push origin main`（触发生产 Deploy）
- **未经用户同意不得**改动页面前端视觉效果（测试/优化/排查中亦同；需改须先征得同意）
- **每次 push 前必须**按 `context/11` 更新 context（至少 `07`，与代码同一 commit）；详见 `.cursor/rules/context-sync-before-push.mdc`
- **Git 覆盖工作区前**：读 `context/10_防灾与备份机制.md`
- **每次更新/Debug 重中之重**：测试脏数据与流程冲突难免，但必须被发现并改进（账本/收据/分期/退款/催款对齐）；见 `.cursor/rules/ledger-consistency-on-debug.mdc`
- **新功能 / 改代码须溯源影响**：评估对其它模块是否引入 bug、错误或性能问题；见 `.cursor/rules/change-impact-analysis.mdc`
- **订单收据 PDF 版式已最终确认**（所有订单统一该视觉；仅内容不同）；见 `.cursor/rules/receipt-pdf-layout.mdc`
- **发给客户的邮件**：一律项目品牌 HTML 模板（禁止裸 HTML）；见 `.cursor/rules/customer-email-brand-template.mdc`
- **后台弹窗**：一律 `customAlert` / `customConfirm`，禁止 `window.alert` / `confirm`；见 `.cursor/rules/admin-alerts-project-style.mdc`
